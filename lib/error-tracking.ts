export function sendErrorToTrackingService(error: Error) {
  // Example: Replace with actual error tracking service integration (e.g., Sentry, LogRocket)
  console.error("Sending error to tracking service:", error);
}