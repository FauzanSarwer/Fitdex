
name: owner-reminders
on:
  schedule:
    - cron: "30 10 * * *"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call reminders endpoint
        env:
          CRON_URL: ${{ secrets.CRON_URL }}
        run: curl -sS "$CRON_URL"
